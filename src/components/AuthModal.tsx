import * as React from 'react';

export interface IProps {
}

export default class AuthModal extends React.Component<IProps, any> {
    render() {
        return (
            <div>
                auth
            </div>
        );
    }
}
